// Styles
import styles from './styles.module.css';
import type { FC } from 'react';

const Chip: FC = () => {
  return (
    <div className={styles['chip-container']}>Button component</div>
  );
};

export default Chip;
